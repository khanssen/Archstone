// eslint.config.cjs (Flat config)
const globals = require("globals");

module.exports = [
  //  1) IGNORES — MUST BE FIRST
  {
    ignores: [
      "Content/HTML Template/**",
      "HTML Template/**",

      "Content/js/**",
      "Content/app/js/**",
      "Content/documentation/assets/**",

      "**/*.min.js",
      "**/gulpfile.js",

      "node_modules/**",
      "dist/**",
      "build/**",
      "coverage/**",
      "vendor/**"
    ],
  },

  //  2) Browser + jQuery code
  {
    files: ["**/*.js"],
    languageOptions: {
      ecmaVersion: "latest",
      sourceType: "script",
      globals: {
        ...globals.browser,
        ...globals.jquery,
        WOW: "readonly",
        google: "readonly",
      },
    },
    rules: {
      semi: ["error", "always"],
      "no-undef": "off",
      "no-unused-vars": ["warn", { args: "none" }],
      "no-redeclare": "warn",
    },
  },

  //  3) Node build scripts (gulp, configs)
  {
    files: ["**/gulpfile.js", "**/*.cjs"],
    languageOptions: {
      ecmaVersion: "latest",
      sourceType: "script",
      globals: {
        ...globals.node,
      },
    },
    rules: {
      semi: ["error", "always"],
      "no-undef": "off",
      "no-unused-vars": ["warn", { args: "none" }],
      "no-redeclare": "warn",
    },
  },
];

